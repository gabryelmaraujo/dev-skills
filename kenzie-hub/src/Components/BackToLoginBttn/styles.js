import styled from "styled-components"

import BackToLoginBttn from "./index"

const BackToLoginBttnStyles = styled.div`

    .backToLoginBttn{
        height: 2.5rem;
        padding: 0 1rem;

        background-color: var(--grey-3);
        color: var(--grey-0);

        border: 1px solid var(--grey-3);
        border-radius: 0.25rem;
    }

`

export default BackToLoginBttnStyles